<!-- ไฟล์คำสั่ง upload รูปภาพ สำหรับดึงไปใช้ -->
<?php
    // แยกชื่อไฟล์กับนามสกุลไฟล์
    $temp = explode('.', $_FILES['img']['name']);
    // สุ่มชื่อไฟล์ใหม่เป็นตัวเลข แล้วต่อกับนามสกุล
    $img = rand() . '.' . end($temp);
    // กำหนดเส้นทางให้ไปที่โฟลเดอร์ upload
    $path = '../upload/' . $img;
?>